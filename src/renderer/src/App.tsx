

function App(): JSX.Element {
  // const ipcHandle = (): void => window.electron.ipcRenderer.send('ping')

  return (
    <div className="bg-blue-300 text-white p-4 cursor hover:text-[20px]">Hello, Tailwind CSS!</div>
  )
}

export default App
